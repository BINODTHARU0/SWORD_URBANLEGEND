local Players = game:GetService("Players") local LocalPlayer = Players.LocalPlayer local function isFriend(player) for _, friend in pairs(LocalPlayer:GetFriendsOnline()) do if friend.VisitorId == player.UserId then return true end end return false end local function onEquipped() while wait(0.1) do for _, player in pairs(Players:GetPlayers()) do if player ~= LocalPlayer and not isFriend(player) then local character = player.Character if character and character:FindFirstChild("Humanoid") then local forceField = character:FindFirstChildOfClass("ForceField")
